//Package particle provides options for generating renderable particle sources.

//Particles are created via generators which are in turn managed via sources.
//Due to particles often being used in high volumes they have their own id spaces and special rules for collision.

package particle
